# 20.16 — Checklist and learning log

**Gate** for Chapter 20. Learning log: **`learning-log/20-cache-and-search.md`**.

---

## Definition of Done

### Redis

- [ ] `REDIS_URL` in `server/.env.example`
- [ ] `shared/redis.js` connects when Redis available
- [ ] API boots when Redis down (fallback)

### Cache

- [ ] `GET /products` uses cache-aside
- [ ] HIT/MISS observable in dev logs
- [ ] TTL documented in README
- [ ] Second identical request hits cache

### Invalidation

- [ ] Product POST/PATCH/DELETE invalidates catalogue cache
- [ ] Price change visible on next GET without waiting TTL

### Search

- [ ] Text index on `name` + `category`
- [ ] `?search=` uses `$text` query
- [ ] explain() or Compass shows index used

### Performance

- [ ] README before/after timing notes

### Prior gates

- [ ] Chapter 19 Week 3 demo complete
- [ ] Chapter 14 public catalogue works

---

## Learning log — mandatory

1. **Cache key:** How do you build cache keys? What invalidation strategy?

2. **Stale trap:** Describe test proving PATCH busts cache (20.11).

3. **Fallback:** What happens when Redis stops? Paste boot log line.

4. **Text index:** Paste explain winning plan or describe index in Compass.

5. **TTL choice:** What TTL did you pick and why?

6. **Stale trap:** If you only used TTL without event invalidation, what bug would customers see after a vendor price change?

7. **Chapter 21 preview:** How will checkout enqueue jobs instead of sending notifications inline?

### Commit

```bash
git add learning-log/20-cache-and-search.md
git commit -m "docs: complete chapter 20 learning log"
```

---

## Gate rule

→ **`21-async-jobs-and-queues/21.01-set-the-scene.md`**

---

## Final gate statement

*"I confirm catalogue caching with invalidation, text search on index, Redis fallback, and performance notes in README. Ready for Chapter 21 async jobs."*
